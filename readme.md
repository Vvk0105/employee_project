Employee CRUD API — Django REST Framework

This is a simple Employee Management REST API built using Django and Django REST Framework  
It supports basic CRUD operations — Create, Read, Update, and Delete employees.

Add new employees (with optional photo upload up to 5 MB)
-List all employees
-Update employee details
-Delete employee records
